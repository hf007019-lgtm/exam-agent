# 参与贡献

感谢你关注 Exam Agent。无论是问题反馈、文档改进、数据格式适配建议，还是功能实现，都欢迎通过 Issue 或 Pull Request 参与。

## 提交 Issue

- 请先搜索现有 Issue，避免重复提交。
- 清楚说明问题现象、期望行为、运行环境与复现步骤。
- 如需附日志或截图，请先移除姓名、准考证号、身份证号、手机号、API Key 等敏感内容。
- 不要在 Issue 中上传真实招考 Excel、CSV、SQLite 数据库或个人敏感信息。

## 提交 Pull Request

1. 从目标分支创建独立功能分支，保持一次 PR 聚焦一个主题。
2. 遵循现有代码风格，避免夹带无关格式化或重构。
3. PR 描述中说明修改点、修改原因、影响范围以及验证方式。
4. 若行为或配置发生变化，请同步更新对应文档和 `.env.example`。
5. 提交前查看 `git diff` 与 `git status`，确认没有意外文件。

## 数据与隐私检查

提交前请重点检查：

- `.env` 及其他本地配置是否包含密钥、Token 或内部服务地址。
- SQLite、`.db`、`.sqlite` 等数据库文件是否被意外纳入。
- Excel、CSV 与导入缓存是否包含真实招考数据。
- 日志、截图和测试样本是否含有姓名、准考证号等个人敏感信息。
- `raw_private/`、`uploads/`、`data/` 等本地目录是否被意外复制或提交。

如需添加示例数据，只能使用完全虚构或充分脱敏、确认可公开的数据，并在 PR 中说明数据来源与处理方式。

## PR 描述建议

```markdown
## 修改内容
- （请填写）

## 修改原因
- （请填写）

## 影响范围
- （请填写）

## 验证方式
- （请填写）

## 数据安全检查
- [ ] 不含 `.env`、密钥或 Token
- [ ] 不含真实 Excel / CSV / SQLite
- [ ] 不含个人敏感信息
```

提交贡献即表示你同意相关内容按项目的 [Apache License 2.0](LICENSE) 发布。
